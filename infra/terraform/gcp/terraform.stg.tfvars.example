## Copy to `terraform.stg.tfvars` (do not commit).
## Then run with:
##   terraform workspace select stg
##   terraform apply -var-file=terraform.stg.tfvars

project_id = "YOUR_GCP_PROJECT_ID"

region = "asia-east1"
zone   = "asia-east1-a"

name_prefix  = "ai-interview-stg"
# Staging can be smaller to reduce cost.
machine_type = "e2-standard-4"

# Security: restrict to your public IP if possible.
# Example: ["203.0.113.10/32"]
ssh_source_ranges = ["0.0.0.0/0"]

# Allow Terraform to stop the instance when resizing.
allow_stopping_for_update = true

# STG work-hours schedule. Asia/Taipei 08:45 start is intentional because
# Compute Engine schedules can run with several minutes of delay.
enable_vm_work_hours_schedule = true
vm_schedule_time_zone         = "Asia/Taipei"
vm_start_schedule             = "45 8 * * *"
vm_stop_schedule              = "0 18 * * *"
